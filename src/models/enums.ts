// For backward compatibility, add new enum only at the last.
export enum Users {
    Admin,
    User
}

export enum Genders {
    Male,
    Female
}

export enum Countries {
    Malaysia,
    Myanmar
}

export enum States {
    KL,
    YGN
}

export enum Cities {
    KL,
    YGN
}

export enum Professions {
    Lecturer,
    Developer,
    DevOps,
    TestEngineer,
    Architect,
    DataScientist,
    DataAnalyst,
    DataEngineer,
    DeliveryManager,
    ProductManager,
    ScrumMaster
}

export enum Studies {
    HighSchool,
    Bachelor,
    Master,
    PHD,
    OnlineCourse
}

export enum Orgs {
    School,
    College,
    University,
    OnlineLearning,
    Company
}

export enum ConnectionRelations {
    Family,
    Relative,
    Friend,
    Classmate,
    Colleague,
    Acquaintance
}

export enum Accesses {
    Public,
    Private
}

export enum PostCategories {
    Post,
    Article,
    Question,
    News,
    Job,
    Event,
    Learning,
    Skill,
    App
}

export enum SkillLevels {
    Expert,
    Experienced,
    Intermediate,
    Beginner
}

export enum DevAppStages {
    Ideation,
    Prototype,
    MVP,
    Alpha,
    Beta,
    Mature
}

export enum DevEventTypes {
    Workshop,
    Meetup,
    Conference,
    Hackathon
}

export enum DevReputations {
    Beginner,
    Enthusiast,
    Geek,
    Influencer
}

export enum NotificationTypes {
    Critical,
    Warning,
    Health,
    Information,
    System
}

export enum NotificationInfoTypes {
    ConnectionRequest,
    Liked,
}

export enum ActionTypes {
    ContinueConnect
}
